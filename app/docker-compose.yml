version: '3.8'

services:
    app:
        container_name: ndr_ui
        image: ndr_ui_image
        build:
            context: .
            dockerfile: docker/Dockerfile
        ports:
            - 8080:80
        env_file:
            - .env
