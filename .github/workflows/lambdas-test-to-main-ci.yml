# .github/workflows/terraform-dev
name: 'Lambdas - Development CI Feature Branch to Main'

on:
  push:
    branches:
    - main
    paths:
    - 'lambdas/**'
  pull_request:
    branches:
    - main
    paths:
    - 'lambdas/**'

permissions:
  pull-requests: write
  id-token: write # This is required for requesting the JWT
  contents: read  # This is required for actions/checkout

jobs:
  python_process_upload_docuement:
    runs-on: ubuntu-latest
    environment: development
    defaults:
      run:
        working-directory: ./lambdas
    strategy:
      matrix:
        python-version: ["3.9"]
  
    steps:

    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install ruff pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Lint with ruff
      run: |
        # stop the build if there are Python syntax errors or undefined names
        ruff --format=github --select=E9,F63,F7,F82 --target-version=py37 .
        # default set of ruff rules with GitHub Annotations
        ruff --format=github --target-version=py37 .
      working-directory: ./lambdas

    - name: Test with pytest
      run: |
        pytest test_create_document_reference
      working-directory: ./lambdas
    
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: ${{ secrets.AWS_ASSUME_ROLE }}
        role-skip-session-tagging: true
        aws-region: ${{ vars.AWS_REGION }}
    
    - name: View AWS Role
      run: aws sts get-caller-identity

    - name: Create release package for Create Document
      run: |
        mkdir package_create_document  
        pip install -r requirements.txt --target ./package_create_document
        cd package_create_document
        zip -r ../package_create_document.zip .
        cd ..
        zip package_create_document.zip lambda_create_document_reference.py
        zip package_create_document.zip utils
      working-directory: ./lambdas
    
    - name: Upload Lambda Function
      uses: appleboy/lambda-action@master
      with:
        aws_region: ${{ vars.AWS_REGION }}
        function_name: ${{ vars.BUILD_ENV }}_CreateDocRefLAmbda
        zip_file: package_create_document.zip
    